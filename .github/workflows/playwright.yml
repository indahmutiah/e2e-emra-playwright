name: Emra Tests

on:
  push:
    branches: [ main, dev ]
  pull_request:
  workflow_run:
    workflows: ["Build Base Docker Image"]
    types:
      - completed

jobs:
  code_analyze:
    runs-on: ubuntu-latest
    name: 🔍 Code Quality Check (ESLint)
    timeout-minutes: 10
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

  build_docker:
    name: 🐳 Build Custom Docker Image
    timeout-minutes: 15
    runs-on: ubuntu-latest
    needs: code_analyze
    if: ${{ github.event.inputs.build_custom_docker == 'true' }}
    container:
      image: docker:24.0.5
      services:
        - docker:24.0.5-dind
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t playwright-custom:${{ github.sha }} .

    outputs:
      docker_image: playwright-custom:${{ github.sha }}

  autotest:
    name: 🧪 Run Playwright Test
    runs-on: ubuntu-latest
    needs: build_docker

    container:
      image: ${{ needs.build_docker.outputs.docker_image }}

    env:
      BASE_URL: ${{ secrets.BASE_URL }}
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - uses: actions/checkout@v4
    
      - name: Run tests
        run: npx playwright test